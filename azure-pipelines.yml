trigger: none

variables:
  configuration : Debug
  platform : X64

# steps:
# - task: MSBuild@1
#   inputs:
#     solution: solution1.sln
#     configuration: $(configuration) # Use the variable
#     platform: $(platform)

steps:
- checkout: none
- task: MSBuild@1
  inputs:
    solution: solution1.sln
    configuration: $(configuration) # Use the variable
    platform: $(platform)


